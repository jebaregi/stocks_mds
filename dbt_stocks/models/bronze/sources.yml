version: 2
sources:
  - name: raw
    description: "Raw landing source for stock quotes ingested into the warehouse"
    database: STOCKS_MDS
    schema: COMMON
    tables:
      - name: BRONZE_STOCKS_QUOTES_RAW
        description: "Bronze-level raw stock quotes (one row per incoming quote message)."
        # Example column tests — uncomment and update column names as needed:
        # columns:
        #   - name: ticker
        #     tests:
        #       - not_null
        #   - name: timestamp
        #     tests:
        #       - not_null